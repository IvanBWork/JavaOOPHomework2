package Interfaces;

/**
 * Интерфейс описывает поведение клиентов,касающееся возврата товара.
 */
public interface iReturnOrder {
    public void ReturnOrder(iActorBehaviour actor); // метод возврата товара
}
